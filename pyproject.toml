[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mining-intel"
version = "0.1.0"
description = "Argentina Mining Intelligence MVP"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
