name: Update mining data

on:
  schedule:
    - cron: "0 12 * * *" # 09:00 ART (UTC-3)
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run update pipeline
        run: python scripts/run_update.py

      - name: Commit updated data if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet -- data/mining.db; then
            git add data/mining.db
            git commit -m "chore: actualizar datos mineros $(date -u +'%Y-%m-%d')"
            git push
          else
            echo "Sin cambios en data/mining.db"
          fi
